# PassStorage
Password storage App.
Includes usage of Sqlite Database.
Database Architecture -> name of the Website, Username, Email-ID , Password (Encrypted using Base64)
RecyclerView shows the values obtained from the database and an option to decrypt the password n number of times until it reaches the end.
